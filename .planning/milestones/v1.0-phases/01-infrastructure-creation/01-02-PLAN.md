---
phase: 01-infrastructure-creation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [ai/price_cache.py]
autonomous: true
requirements: [PC-01, PC-02, PC-03, PC-04, PC-05]
user_setup: []

must_haves:
  truths:
    - "from ai.price_cache import registrar, get_activos, invalidar, limpiar_expirados works"
    - "Concurrent writes from two threads produce no RuntimeError"
    - "from ai import procesar_con_claude still works (ai.py not shadowed)"
    - "No ai/__init__.py exists in the ai/ directory"
  artifacts:
    - path: "ai/price_cache.py"
      provides: "Thread-safe price cache with TTL"
      min_lines: 50
      contains: "threading.RLock"
  key_links: []
---

<objective>
Crear `ai/price_cache.py` — cache RAM thread-safe para precios recientemente actualizados.

Purpose: Eliminar la race condition activa en `_precios_recientes` de `ai.py`. Este módulo es el contenedor seguro; la conexión con `ai.py` ocurre en Fase 2 (Tarea G).
Output: `ai/price_cache.py` con API pública `registrar / get_activos / invalidar / limpiar_expirados`.

⚠️ CRÍTICO: Crear `ai/` directorio SIN `ai/__init__.py`. Si `ai/__init__.py` existe, Python sombrea `ai.py` y rompe todos los callers de `from ai import procesar_con_claude`.
</objective>

<execution_context>
@C:/Users/Dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-creation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear ai/price_cache.py con threading.RLock</name>
  <files>ai/price_cache.py</files>
  <read_first>ai.py</read_first>
  <action>
Crear directorio `ai/` (sin __init__.py) y `ai/price_cache.py` con:

```python
_PRECIO_TTL = 300  # 5 minutos, igual que ai.py original

_cache: dict[str, list[dict]] = {}   # nombre_lower -> lista de entradas
_lock = threading.RLock()             # RLock para re-entrancia segura

def registrar(nombre_lower: str, precio: float, fraccion: str | None = None) -> None: ...
def get_activos() -> dict[str, list[dict]]: ...
def invalidar(nombre_lower: str) -> None: ...
def limpiar_expirados() -> None: ...
```

Cada entrada en la lista: `{"precio": float, "fraccion": str|None, "ts": float}`.
`get_activos()` filtra entradas donde `time.time() - ts < _PRECIO_TTL`.
Todas las mutaciones de `_cache` bajo `with _lock:`.
Logger: `logging.getLogger("ferrebot.ai.price_cache")`.
Imports: `# -- stdlib --` (logging, threading, time) / `# -- terceros --` (ninguno) / `# -- propios --` (ninguno).
Docstrings en español.
  </action>
  <verify>python -c "from ai.price_cache import registrar, get_activos; print('OK')"</verify>
  <acceptance_criteria>
    - ai/price_cache.py existe
    - ai/price_cache.py contiene "threading.RLock"
    - ai/price_cache.py contiene "def registrar"
    - ai/price_cache.py contiene "def get_activos"
    - ai/price_cache.py contiene "def invalidar"
    - ai/price_cache.py contiene "def limpiar_expirados"
    - NO existe ai/__init__.py
  </acceptance_criteria>
  <done>from ai.price_cache import registrar, get_activos, invalidar, limpiar_expirados funciona</done>
</task>

<task type="auto">
  <name>Task 2: Verificar que ai.py no fue sombrado</name>
  <files></files>
  <action>
Ejecutar verificación crítica post-creación:
```bash
python -c "import ai; print(type(ai.procesar_con_claude))"
# Debe imprimir: <class 'coroutine'> o <class 'function'> — NUNCA ModuleNotFoundError
python -c "import main; print('main OK')"
```
Si `ai/__init__.py` fue creado accidentalmente, eliminarlo INMEDIATAMENTE antes de continuar.
  </action>
  <verify>python -c "import ai; print(type(ai.procesar_con_claude))"</verify>
  <acceptance_criteria>
    - Comando "import ai; print(type(ai.procesar_con_claude))" no lanza AttributeError ni ImportError
    - No existe archivo ai/__init__.py
  </acceptance_criteria>
  <done>ai.py original accesible, ai/price_cache.py accesible, sin colisión de namespace</done>
</task>

</tasks>

<verification>
- [ ] python -c "from ai.price_cache import registrar, get_activos, invalidar, limpiar_expirados; print('OK')"
- [ ] python -c "import ai; print(type(ai.procesar_con_claude))"  — no error
- [ ] python -c "import main; print('main OK')"
- [ ] ls ai/ NO muestra __init__.py
</verification>

<success_criteria>
- `from ai.price_cache import get_price, set_price, invalidate_cache` resuelve (o equivalente con nombres reales)
- Concurrent writes desde dos threads no producen RuntimeError
- `from ai import procesar_con_claude` sigue funcionando
- No existe `ai/__init__.py`
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-creation/01-02-SUMMARY.md`
</output>
