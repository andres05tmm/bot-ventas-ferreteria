---
phase: 01-infrastructure-creation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [services/__init__.py, services/catalogo_service.py]
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04]
user_setup: []

must_haves:
  truths:
    - "from services.catalogo_service import * resolves without error"
    - "catalogo_service does not import from ai, handlers, or memoria at module level"
    - "Function signatures are identical to originals in memoria.py"
  artifacts:
    - path: "services/catalogo_service.py"
      provides: "Catalog CRUD functions extracted from memoria.py"
      min_lines: 80
      contains: 'logging.getLogger("ferrebot.services.catalogo")'
    - path: "services/__init__.py"
      provides: "Package marker"
  key_links:
    - from: "services/catalogo_service.py"
      to: "db"
      via: "direct import"
      pattern: "import db"
---

<objective>
Crear `services/catalogo_service.py` con toda la lógica de catálogo extraída de `memoria.py`.

Purpose: Primer paso hacia la separación de responsabilidades en `memoria.py`. En Fase 1 el módulo solo existe en disco — no está conectado a ningún caller aún (integración en Fase 2, Tarea H).
Output: `services/__init__.py` + `services/catalogo_service.py` con las funciones de catálogo.
</objective>

<execution_context>
@C:/Users/Dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-creation/01-RESEARCH.md
@memoria.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear services/__init__.py</name>
  <files>services/__init__.py</files>
  <action>
Crear `services/__init__.py` vacío o con docstring mínimo:
```python
"""Capa de servicios de dominio para FerreBot."""
```
  </action>
  <verify>python -c "import services; print('OK')"</verify>
  <acceptance_criteria>
    - services/__init__.py existe
  </acceptance_criteria>
  <done>services/ es un paquete Python importable</done>
</task>

<task type="auto">
  <name>Task 2: Crear services/catalogo_service.py</name>
  <files>services/catalogo_service.py</files>
  <read_first>memoria.py, db.py, config.py, utils.py, alias_manager.py, fuzzy_match.py</read_first>
  <action>
Crear `services/catalogo_service.py` copiando verbatim las funciones de catálogo de `memoria.py`:

Funciones a incluir (CAT-01):
- `_leer_catalogo_postgres(db_module)` — carga catálogo desde BD
- `buscar_producto_en_catalogo(nombre, catalogo)` — búsqueda con fuzzy
- `buscar_multiples_en_catalogo(nombres, catalogo)` — búsqueda en lote
- `buscar_multiples_con_alias(nombres, catalogo)` — con lookup de alias
- `obtener_precios_como_texto(catalogo)` — formato texto para prompts
- `obtener_info_fraccion_producto(clave, catalogo)` — info de fracciones
- `actualizar_precio_en_catalogo(clave, precio_nuevo, db_module)` — update precio
- `_sincronizar_catalogo_postgres(catalogo, db_module)` — sync completo
- `_upsert_precio_producto_postgres(clave, precio, db_module)` — upsert individual

Reglas (CAT-02, CAT-03):
- Imports permitidos: logging, re, db, config, utils, alias_manager, fuzzy_match
- `cargar_memoria()` se importa lazy dentro de funciones para evitar ciclo (si aplica)
- NUNCA importar de ai, handlers, o memoria a nivel de módulo
- Firmas idénticas a las originales

Header del módulo:
```
# -- stdlib --
import logging
import re

# -- propios --
import config
import db as _db
from utils import _normalizar
from alias_manager import obtener_alias
from fuzzy_match import buscar_fuzzy
```

Logger (CAT-04): `logger = logging.getLogger("ferrebot.services.catalogo")`
Docstrings en español para todas las funciones públicas.
  </action>
  <verify>python -c "from services.catalogo_service import buscar_producto_en_catalogo; print('OK')"</verify>
  <acceptance_criteria>
    - services/catalogo_service.py contiene 'logging.getLogger("ferrebot.services.catalogo")'
    - services/catalogo_service.py contiene "def buscar_producto_en_catalogo"
    - services/catalogo_service.py contiene "def actualizar_precio_en_catalogo"
    - services/catalogo_service.py no contiene "from ai import" ni "from handlers import" ni "from memoria import" a nivel de módulo
  </acceptance_criteria>
  <done>from services.catalogo_service import * resuelve; no hay imports prohibidos a nivel de módulo</done>
</task>

<task type="auto">
  <name>Task 3: Verificar importación limpia y main OK</name>
  <files></files>
  <action>
```bash
python -c "from services.catalogo_service import buscar_producto_en_catalogo, obtener_precios_como_texto; print('catalogo_service OK')"
python -c "import main; print('main OK')"
```
  </action>
  <verify>python -c "import main; print('main OK')"</verify>
  <acceptance_criteria>
    - Importar catalogo_service no lanza ImportError ni RuntimeError
    - python -c "import main" termina sin error
  </acceptance_criteria>
  <done>Módulo importa limpio; bot arranca</done>
</task>

</tasks>

<verification>
- [ ] python -c "from services.catalogo_service import *; print('OK')"
- [ ] services/catalogo_service.py no importa de ai/handlers/memoria a nivel módulo
- [ ] python -c "import main; print('main OK')"
</verification>

<success_criteria>
- `from services.catalogo_service import *` resuelve sin error
- Ningún import de `ai`, `handlers`, o `memoria` a nivel de módulo
- Firmas idénticas a `memoria.py`
- `python main.py` arranca sin errores
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-creation/01-04-SUMMARY.md`
</output>
