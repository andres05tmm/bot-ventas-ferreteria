---
phase: 01-infrastructure-creation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [middleware/__init__.py, middleware/auth.py]
autonomous: true
requirements: [MW-01, MW-02, MW-03, MW-04, MW-05]
user_setup:
  - service: env-var
    why: "Auth decorator reads AUTHORIZED_CHAT_IDS at runtime"
    env_vars:
      - name: AUTHORIZED_CHAT_IDS
        source: "Comma-separated Telegram chat IDs (e.g. 123456,789012). Leave empty for fail-open dev mode."
      - name: RATE_LIMIT_SEGUNDOS
        source: "Sliding window size in seconds (default: 2)"
      - name: RATE_LIMIT_MAX
        source: "Max messages per window per chat (default: 5)"

must_haves:
  truths:
    - "from middleware import protegido works without error"
    - "@protegido wraps an async handler and preserves __name__ (functools.wraps)"
    - "Empty AUTHORIZED_CHAT_IDS allows all chats (fail-open — MW-02)"
    - "Non-empty AUTHORIZED_CHAT_IDS blocks unlisted chat_ids silently"
  artifacts:
    - path: "middleware/__init__.py"
      provides: "Single import path: from middleware import protegido"
      contains: "protegido"
    - path: "middleware/auth.py"
      provides: "@protegido decorator with AUTHORIZED_CHAT_IDS check and rate limiter"
      min_lines: 40
      contains: "functools.wraps"
  key_links:
    - from: "middleware/__init__.py"
      to: "middleware/auth.py"
      via: "re-export"
      pattern: "from middleware.auth import protegido"
---

<objective>
Crear el paquete `middleware/` con el decorador `@protegido` (auth + rate limiting).

Purpose: Proveer una capa de autorización reutilizable para todos los handlers de Telegram. Fase 1 solo crea el módulo — los handlers lo aplican en Fase 2.
Output: `middleware/__init__.py` + `middleware/auth.py` con `@protegido` y `rate_limiter`.
</objective>

<execution_context>
@C:/Users/Dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-creation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear middleware/auth.py con @protegido y rate_limiter</name>
  <files>middleware/auth.py</files>
  <read_first>config.py</read_first>
  <action>
Crear `middleware/auth.py` con:
1. `_get_authorized_ids() -> set[int]` — lee `AUTHORIZED_CHAT_IDS` del env, retorna set vacío si ausente (fail-open MW-02).
2. `protegido(func)` — decorador async con `@functools.wraps(func)` (MW-03) que verifica `update.effective_chat.id` contra el set; si el set está vacío pasa todo.
3. `RateLimiter` — clase con `threading.Lock`, dict `_ventanas: dict[int, list[float]]`, método `check(chat_id) -> bool` con ventana deslizante (MW-04).
4. `rate_limiter = RateLimiter()` — instancia singleton del módulo.
Imports con headers: `# -- stdlib --` (functools, logging, os, threading, time) / `# -- terceros --` (telegram, telegram.ext) / `# -- propios --` (ninguno).
Logger: `logging.getLogger("ferrebot.middleware.auth")`.
Docstrings en español. `except Exception as e:` para estabilidad.
  </action>
  <verify>python -c "from middleware.auth import protegido, rate_limiter; print('OK')"</verify>
  <acceptance_criteria>
    - middleware/auth.py contiene "functools.wraps"
    - middleware/auth.py contiene "AUTHORIZED_CHAT_IDS"
    - middleware/auth.py contiene "threading.Lock"
    - middleware/auth.py contiene 'logging.getLogger("ferrebot.middleware.auth")'
  </acceptance_criteria>
  <done>from middleware.auth import protegido, rate_limiter funciona sin error</done>
</task>

<task type="auto">
  <name>Task 2: Crear middleware/__init__.py con re-export</name>
  <files>middleware/__init__.py</files>
  <action>
Crear `middleware/__init__.py` que re-exporte:
```python
from middleware.auth import protegido, rate_limiter
__all__ = ["protegido", "rate_limiter"]
```
Esto satisface MW-05: `from middleware import protegido` como path único.
  </action>
  <verify>python -c "from middleware import protegido; print('OK')"</verify>
  <acceptance_criteria>
    - middleware/__init__.py contiene "from middleware.auth import protegido"
    - middleware/__init__.py contiene '__all__'
  </acceptance_criteria>
  <done>from middleware import protegido funciona y __name__ del wrapper es el del handler original</done>
</task>

<task type="auto">
  <name>Task 3: Verificar que main.py arranca sin errores</name>
  <files></files>
  <action>
Ejecutar verificación estándar post-commit:
```bash
python -c "from middleware import protegido; print('middleware OK')"
python -c "import main; print('main OK')"
```
Si main.py falla, revisar que middleware/ no haya introducido imports circulares.
  </action>
  <verify>python -c "import main; print('main OK')"</verify>
  <acceptance_criteria>
    - Comando "import main" termina con "main OK" sin excepciones
  </acceptance_criteria>
  <done>Bot arranca limpio con el nuevo middleware presente</done>
</task>

</tasks>

<verification>
- [ ] python -c "from middleware import protegido, rate_limiter; print('OK')"
- [ ] python -c "import main; print('main OK')"
- [ ] middleware/auth.py contiene "functools.wraps" (MW-03)
- [ ] middleware/auth.py contiene "AUTHORIZED_CHAT_IDS" (MW-01)
- [ ] Fail-open con lista vacía verificado
</verification>

<success_criteria>
- `from middleware import protegido` resuelve sin error
- `@protegido` preserva `__name__` del handler (functools.wraps)
- Lista vacía de AUTHORIZED_CHAT_IDS = permitir todos
- `python main.py` arranca sin errores
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-creation/01-01-SUMMARY.md`
</output>
