---
phase: 01-infrastructure-creation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [services/inventario_service.py]
autonomous: true
requirements: [INV-01, INV-02, INV-03, INV-04]
user_setup: []

must_haves:
  truths:
    - "from services.inventario_service import * resolves without error"
    - "descontar_inventario() returns exactly (bool, str|None, float|None)"
    - "inventario_service does not import from ai, handlers, or memoria at module level"
  artifacts:
    - path: "services/inventario_service.py"
      provides: "Inventory logic extracted from memoria.py with preserved return contracts"
      min_lines: 80
      contains: "def descontar_inventario"
  key_links:
    - from: "services/inventario_service.py"
      to: "ventas_state.py"
      via: "descontar_inventario return contract tuple[bool, str|None, float|None]"
      pattern: "def descontar_inventario"
---

<objective>
Crear `services/inventario_service.py` con toda la lógica de inventario extraída de `memoria.py`.

Purpose: Separar inventario del módulo monolítico. En Fase 1 solo existe en disco — la integración ocurre en Fase 2 (Tarea H). El contrato de retorno de `descontar_inventario()` es crítico: lo destruye `ventas_state.py` línea 210.
Output: `services/inventario_service.py` con las funciones de inventario y contrato preservado.
</objective>

<execution_context>
@C:/Users/Dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-creation/01-RESEARCH.md
@memoria.py
@ventas_state.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear services/inventario_service.py</name>
  <files>services/inventario_service.py</files>
  <read_first>memoria.py, ventas_state.py, db.py, config.py</read_first>
  <action>
Crear `services/inventario_service.py` copiando verbatim las funciones de inventario de `memoria.py`:

Funciones a incluir (INV-01):
- `cargar_inventario(db_module)` — lee inventario desde BD (NO llamar cargar_memoria())
- `guardar_inventario(inventario, db_module)` — persiste cambios
- `descontar_inventario(nombre, cantidad, catalogo, inventario, db_module)` — ⚠️ retorna `(bool, str|None, float|None)` EXACTAMENTE
- `_resolver_wayper_inventario(clave, inventario)` — helper interno
- `buscar_clave_inventario(nombre, inventario)` — búsqueda de clave
- `_normalizar_clave_inventario(nombre)` — normalización interna
- `registrar_conteo_inventario(clave, cantidad, db_module)` — registra conteo
- `ajustar_inventario(clave, cantidad_nueva, motivo, db_module)` — ajuste manual
- `verificar_alertas_inventario(inventario)` — chequea stock mínimo
- `_upsert_inventario_producto_postgres(clave, data, db_module)` — upsert BD
- `_KG_INVENTARIO_LINKS: dict` — constante de productos Wayper

⚠️ CONTRATO CRÍTICO (INV-02): `descontar_inventario()` DEBE retornar `(bool, str|None, float|None)`.
`ventas_state.py` línea 210: `descontado, alerta, cantidad_restante = descontar_inventario(...)`.
Cualquier cambio de firma rompe el flujo de ventas silenciosamente.

Reglas (INV-04):
- `cargar_inventario()` lee de `db` directamente — NO importar de `memoria.py` a nivel módulo
- Imports permitidos: logging, db, config, utils
- NUNCA importar de ai, handlers, o memoria a nivel de módulo

Logger: `logger = logging.getLogger("ferrebot.services.inventario")`
Docstrings en español. `except Exception as e:` para estabilidad.
  </action>
  <verify>python -c "from services.inventario_service import descontar_inventario; print('OK')"</verify>
  <acceptance_criteria>
    - services/inventario_service.py contiene "def descontar_inventario"
    - services/inventario_service.py contiene 'logging.getLogger("ferrebot.services.inventario")'
    - services/inventario_service.py no contiene "from ai import" ni "from handlers import" ni "from memoria import" a nivel módulo
    - services/inventario_service.py contiene "def cargar_inventario"
    - services/inventario_service.py contiene "_KG_INVENTARIO_LINKS"
  </acceptance_criteria>
  <done>from services.inventario_service import * resuelve; descontar_inventario retorna (bool, str|None, float|None)</done>
</task>

<task type="auto">
  <name>Task 2: Verificar contrato de retorno y main OK</name>
  <files></files>
  <action>
```bash
python -c "
from services.inventario_service import descontar_inventario
import inspect
sig = inspect.signature(descontar_inventario)
print('Firma:', sig)
print('inventario_service OK')
"
python -c "from services.inventario_service import *; print('all imports OK')"
python -c "import main; print('main OK')"
```
  </action>
  <verify>python -c "import main; print('main OK')"</verify>
  <acceptance_criteria>
    - Importar inventario_service no lanza error
    - python -c "import main" termina sin error
  </acceptance_criteria>
  <done>Módulo importa limpio; contrato verificado; bot arranca</done>
</task>

</tasks>

<verification>
- [ ] python -c "from services.inventario_service import *; print('OK')"
- [ ] services/inventario_service.py contiene "def descontar_inventario"
- [ ] No imports de ai/handlers/memoria a nivel módulo
- [ ] python -c "import main; print('main OK')"
</verification>

<success_criteria>
- `from services.inventario_service import *` resuelve sin error
- `descontar_inventario()` retorna exactamente `(bool, str|None, float|None)`
- Sin imports de `ai`, `handlers`, `memoria` a nivel de módulo
- `python main.py` arranca sin errores
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-creation/01-05-SUMMARY.md`
</output>
