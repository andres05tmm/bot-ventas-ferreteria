---
phase: 05-limpieza
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - start.py
  - memoria.py
  - excel.py
  - routers/ventas.py
  - handlers/callbacks.py
autonomous: true
requirements:
  - CLEAN-02
  - CLEAN-03
  - CLEAN-04
  - CLEAN-05

must_haves:
  truths:
    - "start.py arranca sin llamar a descargar_de_drive('memoria.json', ...)"
    - "guardar_memoria() escribe el JSON local pero no llama a subir_a_drive ni subir_a_drive_urgente"
    - "guardar_venta_excel() escribe el Excel local pero no llama a subir_a_drive(config.EXCEL_FILE)"
    - "guardar_venta_excel() no llama a sheets_agregar_venta"
    - "borrar_venta_excel() no llama a subir_a_drive ni a ninguna función de sheets"
    - "editar_venta() en routers/ventas.py no llama a subir_a_drive ni a sheets_editar_consecutivo"
    - "El callback de borrado en callbacks.py no llama a sheets_borrar_consecutivo; borra de Postgres con db.execute"
    - "El endpoint /ventas/hoy usa Postgres como fuente primaria, Excel como fallback; no llama a sheets_leer_ventas_del_dia"
    - "El Excel watcher thread y _restaurar_memoria() han sido eliminados de start.py"
  artifacts:
    - path: "start.py"
      provides: "Boot sequence sin Drive restore ni excel watcher"
      contains: "_run_historico_safety_net"
    - path: "memoria.py"
      provides: "guardar_memoria sin Drive uploads"
    - path: "excel.py"
      provides: "guardar_venta_excel y borrar_venta_excel sin Drive uploads ni Sheets calls"
    - path: "routers/ventas.py"
      provides: "/ventas/hoy Postgres-primary; editar_venta sin Drive/Sheets"
    - path: "handlers/callbacks.py"
      provides: "Borrado de venta via Postgres DELETE directo"
  key_links:
    - from: "handlers/callbacks.py borrar_ branch"
      to: "ventas + ventas_detalle en Postgres"
      via: "db.execute DELETE"
      pattern: "db\\.execute.*DELETE.*ventas"
    - from: "routers/ventas.py /ventas/hoy"
      to: "_leer_ventas_postgres"
      via: "fuente primaria"
      pattern: "_leer_ventas_postgres.*dias=1"
---

<objective>
Eliminar todas las llamadas a Drive y Sheets para datos estructurados del flujo transaccional.

Purpose: Postgres es ya la fuente de verdad para ventas. El Excel local sigue como fallback de lectura, pero no se sincroniza con Drive. Sheets deja de recibir escrituras. El arranque ya no depende de Drive para obtener memoria.json.

Output: Cinco archivos modificados. Cero llamadas a Drive/Sheets para datos estructurados en el camino crítico.
</objective>

<execution_context>
@C:/Users/Dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-limpieza/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Limpiar start.py — eliminar _restaurar_memoria y excel_watcher</name>
  <files>start.py</files>
  <action>
Aplicar tres eliminaciones quirúrgicas en start.py. NO modificar ningún otro hilo ni la secuencia de arranque de la API, el bot, o el historico_safety_net.

**Eliminar bloque 1 — _restaurar_memoria (líneas 46-63):**
Borrar completamente el bloque que comienza con el comentario `# ── Restaurar memoria.json desde Drive al arrancar`, la definición de la función `_restaurar_memoria()` y la llamada `_restaurar_memoria()` en la línea 63.

Antes:
```python
# ── Restaurar memoria.json desde Drive al arrancar ────────────────────────────
# El archivo NO vive en el repo (está en .gitignore)...
def _restaurar_memoria() -> None:
    from drive import descargar_de_drive
    ruta = config.MEMORIA_FILE
    try:
        ok = descargar_de_drive("memoria.json", ruta)
        if ok:
            log.info("✅ memoria.json restaurado desde Drive")
        else:
            log.warning("⚠️  memoria.json no encontrado en Drive — arrancando vacío")
    except Exception as e:
        log.warning(f"⚠️  No se pudo restaurar memoria.json: {e} — arrancando vacío")

_restaurar_memoria()
```
Después: bloque eliminado. La línea `# ── API en hilo SECUNDARIO...` sigue inmediatamente después de `_db.init_db()`.

**Eliminar bloque 2 — constantes y funciones del excel watcher (líneas 76-166):**
Borrar desde el comentario `# ── Watcher de Excel: sincroniza memoria.json si el Excel cambia en Drive` hasta e incluyendo las líneas:
```python
excel_watcher_thread = threading.Thread(target=_run_excel_watcher, name="excel-watcher", daemon=True)
excel_watcher_thread.start()
log.info("👀 Excel watcher iniciado (intervalo: 2 horas)")
```
Esto incluye: las constantes `EXCEL_WATCH_INTERVAL` y `EXCEL_NOMBRE`, las funciones `_get_excel_modified_time()` y `_run_excel_watcher()`, y la creación+arranque del thread.

El hilo `historico_safety_thread` (líneas 169-201) NO se toca.

**Resultado esperado:** start.py arranca, inicializa DB, lanza la API, lanza el historico_safety_net, borra el webhook, lanza el bot. Sin ninguna mención a Drive para descarga de memoria ni a excel watcher.

Per D-05 (eliminar _restaurar_memoria) y D-08/D-09 (eliminar excel watcher threads).
  </action>
  <verify>
    <automated>cd /c/Users/Dell/Documents/GitHub/bot-ventas-ferreteria && python -c "import ast, sys; ast.parse(open('start.py').read()); print('syntax ok')"</automated>
  </verify>
  <done>
    - start.py no contiene `_restaurar_memoria`, `descargar_de_drive`, `_get_excel_modified_time`, `_run_excel_watcher`, ni `excel_watcher_thread`
    - start.py contiene `historico_safety_thread` (no tocado)
    - Sintaxis Python válida
  </done>
</task>

<task type="auto">
  <name>Task 2: Limpiar memoria.py — eliminar Drive uploads de guardar_memoria</name>
  <files>memoria.py</files>
  <action>
Eliminar el bloque de subida a Drive dentro de `guardar_memoria()`. La función debe seguir escribiendo el JSON local (`memoria.json`) y seguir sincronizando a Postgres. Solo se elimina el tramo de Drive.

Localizar en `guardar_memoria()` el bloque (aprox. líneas 250-256):
```python
    if not _bloquear_subida_drive:
        if urgente:
            from drive import subir_a_drive_urgente
            subir_a_drive_urgente(config.MEMORIA_FILE)
        else:
            from drive import subir_a_drive
            subir_a_drive(config.MEMORIA_FILE)
```
Eliminar ese bloque completo (incluyendo la condición `if not _bloquear_subida_drive:`).

El bloque de sincronización a Postgres que viene después (`import db as _db`, `if _db.DB_DISPONIBLE:` ...) se conserva intacto.

La firma pública de `guardar_memoria(memoria, urgente=False)` NO cambia. El parámetro `urgente` puede permanecer en la firma aunque ya no tenga efecto (para no romper callers), o puede eliminarse si no tiene otros usos — verificar con grep antes de decidir.

Per D-06.
  </action>
  <verify>
    <automated>cd /c/Users/Dell/Documents/GitHub/bot-ventas-ferreteria && python -c "import ast; ast.parse(open('memoria.py').read()); print('syntax ok')" && grep -n "subir_a_drive" memoria.py && echo "grep done"</automated>
  </verify>
  <done>
    - `guardar_memoria()` no contiene ninguna referencia a `subir_a_drive` ni `subir_a_drive_urgente`
    - El JSON local sigue escribiéndose (bloque `with open(config.MEMORIA_FILE, ...)` intacto)
    - La sincronización a Postgres permanece
    - Sintaxis válida
  </done>
</task>

<task type="auto">
  <name>Task 3: Limpiar excel.py, routers/ventas.py y handlers/callbacks.py — eliminar Drive y Sheets calls</name>
  <files>excel.py, routers/ventas.py, handlers/callbacks.py</files>
  <action>
Cuatro operaciones de limpieza en tres archivos. Ejecutar una a la vez, verificar sintaxis después de cada archivo.

---

**A. excel.py — eliminar 8 llamadas a subir_a_drive(config.EXCEL_FILE) y 2 llamadas a sheets (per D-01, D-04)**

Las líneas a eliminar son únicamente las que suben el Excel a Drive o agregan/borran en Sheets. El Excel LOCAL sigue escribiéndose (los `wb.save(config.EXCEL_FILE)` se mantienen).

Líneas a eliminar (buscar y eliminar cada una):

1. En `inicializar_excel()` (~línea 173-174):
   ```python
   from drive import subir_a_drive
   subir_a_drive(config.EXCEL_FILE)
   ```
   Eliminar esas dos líneas. El `print("✅ Archivo Excel creado...")` puede quedar o eliminarse también.

2. En `borrar_cliente_excel()` (~línea 496-497):
   ```python
   from drive import subir_a_drive
   subir_a_drive(config.EXCEL_FILE)
   ```

3. En `guardar_venta_excel()` (~línea 513-514, imports al inicio de la función):
   ```python
   from drive import subir_a_drive
   from sheets import sheets_agregar_venta
   ```
   Eliminar ambas líneas de import.

4. En `guardar_venta_excel()` (~línea 603-612, al final de la función):
   ```python
   wb.save(config.EXCEL_FILE)
   subir_a_drive(config.EXCEL_FILE)

   sheets_agregar_venta(
       consecutivo_final, producto, cantidad, precio_unitario, total, vendedor,
       metodo_pago if metodo_pago else observaciones,
       id_cliente=id_cliente_final, nombre_cliente=nombre_cliente_final,
       codigo_producto=cod_producto_final,
       unidad_medida=datos_base.get("unidad_medida", "Unidad"),
   )
   ```
   Conservar solo `wb.save(config.EXCEL_FILE)`. Eliminar `subir_a_drive(...)` y el bloque `sheets_agregar_venta(...)` completo.

5. En `borrar_venta_excel()` (~línea 619-621, import al inicio):
   ```python
   from drive import subir_a_drive
   from sheets import sheets_borrar_fila
   ```
   Eliminar ambas líneas.

6. En `borrar_venta_excel()` (~línea 698-700, bloque post-save):
   ```python
   wb.save(config.EXCEL_FILE)
   subir_a_drive(config.EXCEL_FILE)
   sheets_borrar_fila(numero_venta)
   ```
   Conservar `wb.save(config.EXCEL_FILE)`. Eliminar `subir_a_drive(...)` y `sheets_borrar_fila(numero_venta)`.

7. En la función de fiados/cuentas (~línea 1032-1034):
   ```python
   wb.save(config.EXCEL_FILE)
   from drive import subir_a_drive
   subir_a_drive(config.EXCEL_FILE)
   ```
   Conservar `wb.save`. Eliminar `from drive import subir_a_drive` y `subir_a_drive(config.EXCEL_FILE)`.

8. En `registrar_compra_en_excel()` (~línea 1081-1083):
   ```python
   wb.save(config.EXCEL_FILE)
   from drive import subir_a_drive
   subir_a_drive(config.EXCEL_FILE)
   ```
   Conservar `wb.save`. Eliminar las otras dos.

9. En `actualizar_hoja_inventario()` (~línea 1163-1165):
   ```python
   wb.save(config.EXCEL_FILE)
   from drive import subir_a_drive
   subir_a_drive(config.EXCEL_FILE)
   ```
   Conservar `wb.save`. Eliminar las otras dos.

También buscar y eliminar el bloque de Sheets en `borrar_venta_excel()` si existe (~líneas 508-536 del Excel interno, el bloque `try: from sheets import _obtener_hoja_sheets, _invalidar_ws_cache ...`).

---

**B. routers/ventas.py — tres cambios (per D-02, D-03, D-04)**

**B1. Eliminar import de sheets_leer_ventas_del_dia (línea 20):**
```python
from sheets import sheets_leer_ventas_del_dia
```
Eliminar esta línea.

**B2. Reescribir /ventas/hoy (líneas 33-95) — Postgres como fuente primaria:**
Reemplazar el bloque completo del endpoint por:
```python
@router.get("/ventas/hoy")
def ventas_hoy():
    try:
        hoy = _hoy()
        filtradas = []
        fuente = "postgres"

        # ── Fuente primaria: Postgres ─────────────────────────────────────────
        try:
            pg_ventas = _leer_ventas_postgres(dias=1)
            if pg_ventas is not None:
                filtradas = [v for v in pg_ventas if str(v.get("fecha", ""))[:10] == hoy]
        except Exception as e_pg:
            logger.warning(f"Postgres no disponible en /ventas/hoy: {e_pg}")

        # ── Fallback: Excel local ─────────────────────────────────────────────
        if not filtradas:
            try:
                ventas_xls = _leer_excel_rango(dias=1)
                filtradas = [v for v in ventas_xls if str(v.get("fecha", ""))[:10] == hoy]
                if filtradas:
                    fuente = "excel_fallback"
            except Exception:
                pass

        # ── Enriquecer con unidad_medida desde el catálogo (solo si falta) ────
        try:
            necesitan = [v for v in filtradas if not v.get("unidad_medida") or v["unidad_medida"] == "Unidad"]
            if necesitan and os.path.exists(config.MEMORIA_FILE):
                with open(config.MEMORIA_FILE, encoding="utf-8") as _f:
                    _mem = json.load(_f)
                catalogo = _mem.get("catalogo", {})

                def _unidad_para(nombre_prod: str) -> str:
                    if not nombre_prod:
                        return "Unidad"
                    n = nombre_prod.lower().strip()
                    for key, prod in catalogo.items():
                        if prod.get("nombre", "").lower().strip() == n or key == n.replace(" ", "_"):
                            return prod.get("unidad_medida", "Unidad") or "Unidad"
                    return "Unidad"

                for v in necesitan:
                    v["unidad_medida"] = _unidad_para(v.get("producto", ""))
        except Exception:
            pass

        return {"fecha": hoy, "ventas": filtradas, "total": len(filtradas), "fuente": fuente}
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))
```

**B3. En `eliminar_linea_venta()` (~líneas 500-536):**
Eliminar la llamada `subir_a_drive(config.EXCEL_FILE)` y el bloque de Sheets que empieza con `# Borrar de Sheets también` hasta el `except Exception: pass` que lo cierra.

Eliminar también el import al inicio de esa función: `from drive import subir_a_drive`.

**B4. En `editar_venta()` (~líneas 569-641):**
- Eliminar `from drive import subir_a_drive` del import al inicio de la función (línea ~569).
- Eliminar `from sheets import sheets_editar_consecutivo` del import (línea ~570).
- Eliminar la llamada `subir_a_drive(config.EXCEL_FILE)` (~línea 633).
- Eliminar el bloque de Sheets:
  ```python
  # ── Sincronizar a Google Sheets ───────────────────────────────
  try:
      sheets_editar_consecutivo(numero, cambios, producto_original=body.producto_original)
  except Exception:
      pass   # No fallar la respuesta si Sheets falla
  ```

Ahora añadir Postgres UPDATE al final del bloque `if actualizadas:`, antes del `return`. Insertar justo después de `recalcular_caja_desde_excel()`:
```python
            # ── Sincronizar a Postgres ────────────────────────────────────
            try:
                import db as _db
                if _db.DB_DISPONIBLE and cambios:
                    set_parts = []
                    params = []
                    campo_col_pg = {
                        "producto":        "producto_nombre",
                        "cantidad":        "cantidad",
                        "precio_unitario": "precio_unitario",
                        "total":           "total",
                    }
                    cabecera_col_pg = {
                        "metodo_pago": "metodo_pago",
                        "cliente":     "cliente_nombre",
                        "vendedor":    "vendedor",
                    }
                    # Actualizar ventas_detalle
                    det_parts = []
                    det_params = []
                    for campo, col in campo_col_pg.items():
                        if campo in cambios:
                            det_parts.append(f"{col} = %s")
                            det_params.append(cambios[campo])
                    if det_parts:
                        det_params.append(numero)
                        _db.execute(
                            f"UPDATE ventas_detalle SET {', '.join(det_parts)} WHERE venta_id = (SELECT id FROM ventas WHERE consecutivo = %s LIMIT 1)",
                            det_params,
                        )
                    # Actualizar ventas (cabecera)
                    cab_parts = []
                    cab_params = []
                    for campo, col in cabecera_col_pg.items():
                        if campo in cambios:
                            cab_parts.append(f"{col} = %s")
                            cab_params.append(cambios[campo])
                    if cab_parts:
                        cab_params.append(numero)
                        _db.execute(
                            f"UPDATE ventas SET {', '.join(cab_parts)} WHERE consecutivo = %s",
                            cab_params,
                        )
            except Exception as e_pg:
                logger.warning(f"No se pudo sincronizar edición a Postgres (no crítico): {e_pg}")
```

---

**C. handlers/callbacks.py — reemplazar Sheets delete por Postgres DELETE (per D-03)**

En el bloque `elif data.startswith("borrar_"):`, reemplazar el bloque Sheets (~líneas 299-315):

Antes:
```python
        if confirm == "si" and numero:
            # Borrar de Sheets primero (si está disponible)
            sheets_borradas = 0
            if config.SHEETS_ID and config.SHEETS_DISPONIBLE:
                from sheets import sheets_borrar_consecutivo
                sheets_borradas, _ = await asyncio.to_thread(sheets_borrar_consecutivo, numero)

            # También borrar del Excel local
            exito, msg = await asyncio.to_thread(borrar_venta_excel, numero)

            # Mensaje de confirmación
            if sheets_borradas > 0:
                await query.edit_message_text(f"✅ Consecutivo #{numero} eliminado ({sheets_borradas} productos borrados de Sheets).")
            elif exito:
                await query.edit_message_text(msg)
            else:
                await query.edit_message_text(f"✅ Consecutivo #{numero} eliminado.")
```

Después:
```python
        if confirm == "si" and numero:
            # Borrar de Postgres
            pg_borradas = 0
            try:
                import db as _db
                if _db.DB_DISPONIBLE:
                    rows = await asyncio.to_thread(
                        _db.execute,
                        "DELETE FROM ventas_detalle WHERE venta_id = (SELECT id FROM ventas WHERE consecutivo = %s)",
                        [numero],
                    )
                    await asyncio.to_thread(
                        _db.execute,
                        "DELETE FROM ventas WHERE consecutivo = %s",
                        [numero],
                    )
                    pg_borradas = 1
            except Exception as e_pg:
                import logging as _log
                _log.getLogger("ferrebot.callbacks").warning(f"Error borrando de Postgres: {e_pg}")

            # Borrar del Excel local también
            exito, msg = await asyncio.to_thread(borrar_venta_excel, numero)

            if pg_borradas > 0 or exito:
                await query.edit_message_text(f"✅ Consecutivo #{numero} eliminado.")
            else:
                await query.edit_message_text(f"✅ Consecutivo #{numero} eliminado.")
```

  </action>
  <verify>
    <automated>cd /c/Users/Dell/Documents/GitHub/bot-ventas-ferreteria && python -c "import ast; [ast.parse(open(f).read()) and print(f, 'ok') for f in ['excel.py','routers/ventas.py','handlers/callbacks.py']]"</automated>
  </verify>
  <done>
    - `grep -rn "subir_a_drive" excel.py routers/ventas.py handlers/callbacks.py` retorna vacío (o solo comentarios)
    - `grep -rn "sheets_agregar_venta\|sheets_borrar_fila\|sheets_leer_ventas_del_dia\|sheets_borrar_consecutivo\|sheets_editar_consecutivo" excel.py routers/ventas.py handlers/callbacks.py` retorna vacío
    - Los tres archivos tienen sintaxis Python válida
    - El mensaje de confirmación de borrado sigue funcionando
  </done>
</task>

</tasks>

<verification>
Después de completar las tres tareas, ejecutar:

```bash
cd /c/Users/Dell/Documents/GitHub/bot-ventas-ferreteria

# Verificar sintaxis de todos los archivos modificados
python -c "
import ast
files = ['start.py', 'memoria.py', 'excel.py', 'routers/ventas.py', 'handlers/callbacks.py']
for f in files:
    try:
        ast.parse(open(f).read())
        print(f'OK: {f}')
    except SyntaxError as e:
        print(f'ERROR: {f}: {e}')
"

# Verificar ausencia de Drive calls para datos estructurados
grep -n "subir_a_drive\|descargar_de_drive" start.py memoria.py excel.py routers/ventas.py handlers/callbacks.py

# Verificar ausencia de Sheets writes
grep -n "sheets_agregar_venta\|sheets_borrar_fila\|sheets_borrar_consecutivo\|sheets_editar_consecutivo\|sheets_leer_ventas_del_dia" start.py memoria.py excel.py routers/ventas.py handlers/callbacks.py

# Verificar que _restaurar_memoria fue eliminada
grep -n "_restaurar_memoria\|excel_watcher" start.py

# Verificar que drive.py sigue existiendo
ls drive.py
```

Los primeros dos grep deben retornar vacío. El tercero también. El cuarto debe retornar vacío. drive.py debe existir.
</verification>

<success_criteria>
1. Ninguno de los 5 archivos modificados contiene `subir_a_drive(config.EXCEL_FILE)` ni `subir_a_drive(config.MEMORIA_FILE)`
2. `start.py` no contiene `_restaurar_memoria`, `excel_watcher_thread`, ni `_run_excel_watcher`
3. `memoria.py` no contiene `subir_a_drive_urgente` en el cuerpo de `guardar_memoria()`
4. `routers/ventas.py` endpoint `/ventas/hoy` llama a `_leer_ventas_postgres` como primera fuente
5. `handlers/callbacks.py` borrado de consecutivo usa `db.execute DELETE` en lugar de `sheets_borrar_consecutivo`
6. `drive.py` permanece en el codebase (no fue tocado)
7. Los 5 archivos tienen sintaxis Python válida
</success_criteria>

<output>
After completion, create `.planning/phases/05-limpieza/05-01-SUMMARY.md` with:
- Files modified and key changes made
- Any deviations from the plan with reasons
- Grep results confirming Drive/Sheets calls eliminated
- Syntax check output
</output>
