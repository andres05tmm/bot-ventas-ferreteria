---
phase: 05-limpieza
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - routers/ventas.py
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-06

must_haves:
  truths:
    - "GET /export/ventas.xlsx responde con Content-Type application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
    - "El archivo Excel descargado contiene una hoja llamada 'Ventas'"
    - "La hoja tiene exactamente las columnas: consecutivo, fecha, hora, cliente, producto, cantidad, unidad_medida, precio_unitario, total, vendedor, metodo_pago"
    - "Las filas se ordenan por fecha DESC, consecutivo DESC"
    - "Si Postgres no está disponible el endpoint retorna 503 con mensaje claro, sin crash"
    - "test_suite.py pasa 1096+ tests"
  artifacts:
    - path: "routers/ventas.py"
      provides: "GET /export/ventas.xlsx endpoint"
      contains: "/export/ventas.xlsx"
  key_links:
    - from: "GET /export/ventas.xlsx"
      to: "ventas JOIN ventas_detalle en Postgres"
      via: "db.query_all con JOIN y ORDER BY"
      pattern: "query_all.*ventas.*ventas_detalle"
    - from: "query results"
      to: "StreamingResponse con BytesIO"
      via: "openpyxl Workbook en memoria"
      pattern: "StreamingResponse.*BytesIO"
---

<objective>
Añadir el endpoint GET /export/ventas.xlsx que genera el Excel de ventas on-demand desde Postgres, y verificar que toda la suite de tests pasa tras la limpieza de la fase 5.

Purpose: Reemplaza la sincronización continua de ventas.xlsx a Drive. Cualquier usuario con acceso puede descargar un Excel actualizado al instante desde Postgres. El Excel no vive en Drive — se genera en memoria y se descarga directamente.

Output: Un endpoint nuevo en routers/ventas.py. test_suite.py pasa 1096+ tests confirmando que la limpieza no rompió nada.
</objective>

<execution_context>
@C:/Users/Dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-limpieza/05-CONTEXT.md
@.planning/phases/05-limpieza/05-01-SUMMARY.md
</context>

<interfaces>
<!-- Contratos que el executor necesita. Extraídos del codebase. -->

From routers/shared.py — helper de Postgres ya disponible:
```python
def _leer_ventas_postgres(dias: int | None = None, mes_actual: bool = False) -> list[dict] | None:
    """
    Retorna lista de dicts con keys: num, fecha, hora, cliente, id_cliente, producto,
    cantidad, unidad_medida, precio_unitario, total, alias, vendedor, metodo
    Retorna None si Postgres no disponible.
    """
```

From db.py — patrón establecido (lazy import dentro de funciones):
```python
import db as _db
rows = _db.query_all("SELECT ... FROM ventas v JOIN ventas_detalle d ON d.venta_id = v.id ORDER BY v.fecha DESC, v.consecutivo DESC", [])
# rows es lista de dicts (RealDictRow compatible con dict)
```

From routers/ventas.py — imports ya presentes en el archivo:
```python
from fastapi.responses import FileResponse, StreamingResponse
import openpyxl  # ya en requirements.txt
```

Columnas requeridas (D-12):
consecutivo, fecha, hora, cliente, producto, cantidad, unidad_medida, precio_unitario, total, vendedor, metodo_pago

Mapeo query → columna Excel:
- v.consecutivo        → "consecutivo"
- v.fecha::text        → "fecha"
- v.hora::text         → "hora"
- v.cliente_nombre     → "cliente"
- d.producto_nombre    → "producto"
- d.cantidad           → "cantidad"
- d.unidad_medida      → "unidad_medida"
- d.precio_unitario    → "precio_unitario"
- d.total              → "total"
- v.vendedor           → "vendedor"
- v.metodo_pago        → "metodo_pago"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Añadir GET /export/ventas.xlsx a routers/ventas.py</name>
  <files>routers/ventas.py</files>
  <action>
Añadir el endpoint al final de `routers/ventas.py`, antes del último bloque (si hay) o al final del archivo. No modificar ningún endpoint existente.

El endpoint debe:
1. Usar lazy import de `db` dentro de la función (patrón establecido en el proyecto — evita circular import)
2. Ejecutar un JOIN ventas + ventas_detalle sin filtro de fecha, ordenado por `fecha DESC, consecutivo DESC`
3. Construir un Workbook openpyxl en memoria (BytesIO), una sola hoja llamada "Ventas"
4. Encabezados en fila 1 con las 11 columnas en el orden exacto de D-12
5. Retornar `StreamingResponse` con el BytesIO y el Content-Type correcto
6. Si Postgres no disponible o falla: retornar HTTPException 503

Añadir al final del archivo:

```python
# ── Export Excel on-demand ─────────────────────────────────────────────────────

@router.get("/export/ventas.xlsx")
def export_ventas_xlsx():
    """
    Genera y descarga un archivo Excel con todas las ventas desde Postgres.
    Una sola hoja plana 'Ventas'. Ordenado por fecha DESC, consecutivo DESC.
    Per D-10, D-11, D-12, D-13.
    """
    import io
    import openpyxl
    from openpyxl.styles import Font, PatternFill, Alignment
    from openpyxl.utils import get_column_letter

    try:
        import db as _db
        if not _db.DB_DISPONIBLE:
            raise HTTPException(status_code=503, detail="Base de datos no disponible")

        sql = """
            SELECT
                v.consecutivo,
                v.fecha::text          AS fecha,
                COALESCE(v.hora::text, '')          AS hora,
                COALESCE(v.cliente_nombre, 'Consumidor Final') AS cliente,
                d.producto_nombre      AS producto,
                d.cantidad::text       AS cantidad,
                COALESCE(d.unidad_medida, 'Unidad') AS unidad_medida,
                COALESCE(d.precio_unitario, 0)::float AS precio_unitario,
                COALESCE(d.total, 0)::float           AS total,
                COALESCE(v.vendedor, '')    AS vendedor,
                COALESCE(v.metodo_pago, '') AS metodo_pago
            FROM ventas v
            JOIN ventas_detalle d ON d.venta_id = v.id
            ORDER BY v.fecha DESC, v.consecutivo DESC
        """
        rows = _db.query_all(sql, [])

    except HTTPException:
        raise
    except Exception as e:
        logger.error(f"Error generando export ventas.xlsx: {e}")
        raise HTTPException(status_code=503, detail=f"Error consultando base de datos: {e}")

    # ── Construir Excel en memoria ────────────────────────────────────────────
    wb = openpyxl.Workbook()
    ws = wb.active
    ws.title = "Ventas"

    COLUMNAS = [
        "consecutivo", "fecha", "hora", "cliente", "producto",
        "cantidad", "unidad_medida", "precio_unitario", "total",
        "vendedor", "metodo_pago",
    ]

    # Encabezados con estilo
    header_font  = Font(bold=True, color="FFFFFF")
    header_fill  = PatternFill("solid", fgColor="1B56E1")
    header_align = Alignment(horizontal="center")
    for col_idx, nombre in enumerate(COLUMNAS, 1):
        celda = ws.cell(row=1, column=col_idx, value=nombre.upper().replace("_", " "))
        celda.font      = header_font
        celda.fill      = header_fill
        celda.alignment = header_align

    # Anchos de columna (aprox)
    anchos = [14, 12, 8, 25, 35, 10, 14, 16, 14, 18, 14]
    for col_idx, ancho in enumerate(anchos, 1):
        ws.column_dimensions[get_column_letter(col_idx)].width = ancho

    # Filas de datos
    alt_fill = PatternFill("solid", fgColor="EFF6FF")
    for row_idx, row in enumerate(rows, 2):
        for col_idx, clave in enumerate(COLUMNAS, 1):
            valor = row.get(clave)
            celda = ws.cell(row=row_idx, column=col_idx, value=valor)
            celda.alignment = Alignment(horizontal="center")
            if row_idx % 2 == 0:
                celda.fill = alt_fill
            # Formato numérico para columnas de dinero
            if clave in ("precio_unitario", "total"):
                celda.number_format = "$#,##0.00"

    # ── Serializar a BytesIO ──────────────────────────────────────────────────
    buffer = io.BytesIO()
    wb.save(buffer)
    buffer.seek(0)

    return StreamingResponse(
        buffer,
        media_type="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
        headers={"Content-Disposition": "attachment; filename=ventas.xlsx"},
    )
```

  </action>
  <verify>
    <automated>cd /c/Users/Dell/Documents/GitHub/bot-ventas-ferreteria && python -c "import ast; ast.parse(open('routers/ventas.py').read()); print('syntax ok')" && grep -n "export/ventas.xlsx" routers/ventas.py</automated>
  </verify>
  <done>
    - `grep -n "export/ventas.xlsx" routers/ventas.py` retorna al menos una línea con el decorator `@router.get`
    - `grep -n "StreamingResponse.*BytesIO\|BytesIO.*StreamingResponse\|buffer" routers/ventas.py` retorna líneas del endpoint
    - Sintaxis válida
  </done>
</task>

<task type="auto">
  <name>Task 2: Ejecutar test_suite.py y confirmar 1096+ tests pasan</name>
  <files></files>
  <action>
Ejecutar la suite de tests completa para confirmar que los cambios de la fase 5 no rompieron nada.

```bash
cd /c/Users/Dell/Documents/GitHub/bot-ventas-ferreteria
python -m pytest test_suite.py -v --tb=short 2>&1 | tail -30
```

Si algún test falla:

1. Leer el traceback completo del test que falla.
2. Identificar si el fallo es causado por un cambio de esta fase (eliminación de Drive/Sheets call) o por algo preexistente.
3. Si es causado por esta fase: corregir el archivo afectado de forma mínima (preservar el comportamiento esperado por el test sin reintroducir llamadas a Drive/Sheets).
4. Si es preexistente: documentarlo en el SUMMARY y continuar.

El criterio de éxito es `1096 passed` (o más). Si hay tests que fallan por razones no relacionadas con esta fase (ya fallaban antes), documentarlos en el SUMMARY pero no bloquear la finalización del plan.

**Errores comunes a anticipar y cómo resolverlos:**

- `ImportError: cannot import name 'sheets_leer_ventas_del_dia' from 'sheets'` en tests que importan `routers/ventas.py` — asegurarse de que el import fue eliminado del archivo en Task del plan 05-01.
- `AttributeError` en `borrar_venta_excel` relacionado con la ausencia del bloque de Sheets — el test puede estar verificando el mensaje de retorno; si el mensaje cambió ligeramente, ajustar el mensaje de retorno de `borrar_venta_excel` para que sea compatible.
- Tests que mockean `subir_a_drive` — si el mock ya no es necesario porque la llamada fue eliminada, el test puede simplemente pasar con el mock sin usar (no es un error).
  </action>
  <verify>
    <automated>cd /c/Users/Dell/Documents/GitHub/bot-ventas-ferreteria && python -m pytest test_suite.py --tb=no -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - La línea final del output muestra `X passed` donde X >= 1096
    - Si hay failures: son documentados en el SUMMARY con traceback y se confirma que son preexistentes (no causados por esta fase)
  </done>
</task>

</tasks>

<verification>
Después de completar las dos tareas:

```bash
cd /c/Users/Dell/Documents/GitHub/bot-ventas-ferreteria

# 1. Verificar endpoint existe en el router
grep -n "export/ventas.xlsx\|StreamingResponse\|BytesIO" routers/ventas.py

# 2. Verificar sintaxis final de todos los archivos de la fase
python -c "
import ast
files = ['start.py', 'memoria.py', 'excel.py', 'routers/ventas.py', 'handlers/callbacks.py']
for f in files:
    try:
        ast.parse(open(f).read())
        print(f'OK: {f}')
    except SyntaxError as e:
        print(f'ERROR: {f}: {e}')
"

# 3. Test suite
python -m pytest test_suite.py --tb=no -q 2>&1 | tail -5

# 4. Verificar que Drive sigue en el codebase (para fotos de facturas)
python -c "import drive; print('drive.py importable:', True)"
```
</verification>

<success_criteria>
1. `GET /export/ventas.xlsx` existe en routers/ventas.py con `StreamingResponse` y `BytesIO`
2. La query SQL usa `JOIN ventas_detalle` y `ORDER BY v.fecha DESC, v.consecutivo DESC`
3. La hoja Excel se llama "Ventas" y tiene las 11 columnas de D-12
4. Si Postgres no disponible: endpoint retorna 503, no 500 ni crash
5. `python -m pytest test_suite.py --tb=no -q` reporta >= 1096 passed
6. `drive.py` es importable (no fue eliminado)
</success_criteria>

<output>
After completion, create `.planning/phases/05-limpieza/05-02-SUMMARY.md` with:
- Confirmation that endpoint was added and its location in the file
- Test suite results (exact count: X passed, Y failed, Z errors)
- Any test failures and whether they are preexisting or caused by this phase
- Final state: CLEAN-01 through CLEAN-06 status
</output>
