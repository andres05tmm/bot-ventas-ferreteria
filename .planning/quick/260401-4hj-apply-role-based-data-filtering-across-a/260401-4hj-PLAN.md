---
phase: quick
plan: 260401-4hj
type: execute
wave: 1
depends_on: []
files_modified: 
  - routers/deps.py
  - routers/ventas.py
  - routers/caja.py
  - routers/proveedores.py
  - routers/reportes.py
  - routers/historico.py
  - routers/catalogo.py
  - routers/chat.py
  - frontend/src/hooks/useAuth.js
  - frontend/src/components/Dashboard/TabResumen.jsx
  - frontend/src/components/Dashboard/TabVentas.jsx
  - frontend/src/components/Dashboard/TabHistoricoVentas.jsx
  - frontend/src/components/Dashboard/TabCaja.jsx
  - frontend/src/components/Dashboard/TabGastos.jsx
  - frontend/src/components/Dashboard/TabCompras.jsx
  - frontend/src/components/Dashboard/TabInventario.jsx
  - frontend/src/components/Dashboard/TabProveedores.jsx
autonomous: true
requirements: []
---

<objective>
Apply role-based data filtering across all FastAPI routers and React dashboard components.

Enforce: admins see all data, vendedores see only their own usuario_id data.

Purpose: Implement authorization layer on top of JWT authentication (260401-24l) to prevent vendedores from accessing other users' sales, expenses, and purchases.

Output: 
- Backend JWT dependency injection via Depends(get_current_user) and Depends(get_filtro_usuario)
- SQL WHERE clause filtering for vendedores on: ventas, gastos, compras
- Frontend authFetch() helper replacing all dashboard fetch() calls
- Zero data leakage between users
</objective>

<execution_context>
@C:/Users/Dell/Documents/GitHub/bot-ventas-ferreteria/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell/Documents/GitHub/bot-ventas-ferreteria/.claude/get-shit-done/templates/summary.md

**Prior context:** JWT auth completed (260401-24l) — tokens include usuario_id, rol (admin|vendedor). Database schema updated (260401-0dm) — usuarios table has id, telegram_id, nombre, rol.
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create JWT dependency module (routers/deps.py) and inject into all backend routers</name>
  <files>routers/deps.py</files>
  <action>
Create new file `routers/deps.py` with two FastAPI dependencies:

1. `get_current_user(authorization: str = Header(None))` — validates JWT token, returns payload dict {usuario_id, telegram_id, nombre, rol}
   - If no header or invalid format → HTTPException(401, "No autenticado")
   - On jwt.ExpiredSignatureError → HTTPException(401, "Sesión expirada")
   - On jwt.InvalidSignatureError or other → HTTPException(401, "Token inválido")
   - Use `os.environ["SECRET_KEY"]` as per 260401-24l
   - Decode with algorithms=["HS256"]

2. `get_filtro_usuario(current_user=Depends(get_current_user))` — returns usuario_id filtering helper
   - If current_user["rol"] == "vendedor" → return current_user["usuario_id"] (int)
   - If current_user["rol"] == "admin" → return None (no filter)
   - Used to conditionally add `AND v.usuario_id = %s` to WHERE clauses

Import structure:
```python
# -- stdlib --
import os
import jwt
import logging

# -- terceros --
from fastapi import Header, HTTPException, Depends

logger = logging.getLogger("ferrebot.routers.deps")
```

Verify: File exists, both functions importable via `from routers.deps import get_current_user, get_filtro_usuario`.
  </action>
  <verify>
python -c "from routers.deps import get_current_user, get_filtro_usuario; print('deps OK')"
  </verify>
  <done>routers/deps.py created with both dependencies, importable without errors</done>
</task>

<task type="auto">
  <name>Task 2: Apply role-based filtering to backend routers (ventas, caja, proveedores, others)</name>
  <files>routers/ventas.py, routers/caja.py, routers/proveedores.py, routers/reportes.py, routers/historico.py, routers/catalogo.py, routers/chat.py</files>
  <action>
Modify each router file with JWT injection per these rules:

**routers/ventas.py:**
- Import: `from routers.deps import get_current_user, get_filtro_usuario`
- GET /ventas/hoy, /ventas/semana, /ventas/top, /ventas/top2, /ventas/resumen, /export/ventas.xlsx:
  - Add parameter: `filtro: int | None = Depends(get_filtro_usuario)`
  - In query WHERE clause: if filtro is not None, append `AND v.usuario_id = %s` and add filtro to params tuple
  - Example: `WHERE v.fecha >= %s AND v.usuario_id = %s` with params `(date_start, filtro)` when filtro set
  - If filtro is None, use original query (admin sees all)

**routers/caja.py:**
- Import: `from routers.deps import get_current_user`
- GET /caja, GET /gastos, GET /compras:
  - Add parameter: `current_user=Depends(get_current_user)`
  - /caja: NO data filtering (shared cash summary), just require auth
  - /gastos: if current_user["rol"] == "vendedor", add `AND usuario_id = %s` with current_user["usuario_id"]
  - /compras: if current_user["rol"] == "vendedor", add `AND usuario_id = %s` with current_user["usuario_id"]

**routers/proveedores.py:**
- Import: `from routers.deps import get_current_user`
- All GET/POST endpoints:
  - Add parameter: `current_user=Depends(get_current_user)`
  - If current_user["rol"] != "admin": raise HTTPException(403, "Solo admin")
  - Proveedores data is admin-only

**routers/reportes.py, routers/historico.py, routers/catalogo.py, routers/chat.py:**
- Import: `from routers.deps import get_current_user`
- All GET endpoints:
  - Add parameter: `current_user=Depends(get_current_user)` (require auth only, no filtering)
  - These endpoints return shared data (catalogo/kardex, historico summaries, chat logs)
  - No usuario_id filtering needed

Read each file first to find actual endpoint signatures and WHERE clause locations. Do NOT add filtro param to non-GET endpoints.

Verify: All routers import cleanly, each GET endpoint has the dependency injected.
  </action>
  <verify>
python -c "from routers import ventas, caja, proveedores, reportes, historico, catalogo, chat; print('routers OK')"
  </verify>
  <done>All backend routers updated with JWT injection; GET endpoints enforce role-based filtering per table (vendedores see only own usuario_id data for ventas/gastos/compras; admins see all; shared endpoints require auth only)</done>
</task>

<task type="auto">
  <name>Task 3: Add authFetch() helper to frontend and replace all dashboard fetch() calls</name>
  <files>frontend/src/hooks/useAuth.js, frontend/src/components/Dashboard/TabResumen.jsx, frontend/src/components/Dashboard/TabVentas.jsx, frontend/src/components/Dashboard/TabHistoricoVentas.jsx, frontend/src/components/Dashboard/TabCaja.jsx, frontend/src/components/Dashboard/TabGastos.jsx, frontend/src/components/Dashboard/TabCompras.jsx, frontend/src/components/Dashboard/TabInventario.jsx, frontend/src/components/Dashboard/TabProveedores.jsx</files>
  <action>
**Step 1: Update frontend/src/hooks/useAuth.js**

Add two new exports:

```javascript
export function getAuthHeaders() {
  const token = getToken();
  return {
    "Authorization": `Bearer ${token}`,
    "Content-Type": "application/json",
  };
}

export async function authFetch(url, options = {}) {
  const token = getToken();
  const res = await fetch(url, {
    ...options,
    headers: {
      ...options.headers,
      "Authorization": `Bearer ${token}`,
      "Content-Type": "application/json",
    }
  });
  
  // If 401 Unauthorized, token expired — clear storage and redirect to login
  if (res.status === 401) {
    localStorage.removeItem('ferrebot_token');
    localStorage.removeItem('ferrebot_user');
    window.location.href = '/login';
    return null;
  }
  
  return res;
}
```

**Step 2: Find and replace all fetch() calls in dashboard components**

For each file in the list (TabResumen, TabVentas, TabHistoricoVentas, TabCaja, TabGastos, TabCompras, TabInventario, TabProveedores):
1. Read the file first to locate all fetch() calls
2. At top of file, add import: `import { authFetch } from "../hooks/useAuth";` (adjust path if needed)
3. Replace each bare `fetch(url, options)` with `authFetch(url, options)`
4. If response null (401 redirect), wrap in null check or rely on window.location.href redirect

Example pattern:
- BEFORE: `const res = await fetch('/api/ventas/hoy');`
- AFTER: `const res = await authFetch('/api/ventas/hoy');`

Read each component file first to determine exact fetch() locations and structure. Do NOT modify Login.jsx or ProtectedRoute.jsx.

Verify: Dashboard components import authFetch, all fetch() calls replaced, no bare fetch() remains in dashboard tabs.
  </action>
  <verify>
grep -r "fetch(" frontend/src/components/Dashboard/Tab*.jsx | grep -v "authFetch" | wc -l
# Should return 0 (no remaining bare fetch calls in Tab components)

# Verify authFetch is exported from useAuth
grep -n "export.*authFetch" frontend/src/hooks/useAuth.js
  </verify>
  <done>authFetch() helper created in useAuth.js; all dashboard Tab components import and use authFetch() instead of fetch(); 401 responses redirect to login and clear tokens</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Backend routes enforce auth:**
   - Call GET /api/ventas/hoy without Authorization header → 401 "No autenticado"
   - Call with valid JWT token → 200 with data
   - Vendedor token sees only own usuario_id sales
   - Admin token sees all sales

2. **Frontend sends Authorization header:**
   - Open DevTools Network tab
   - Load dashboard
   - Inspect any GET request (e.g., /api/ventas/hoy)
   - Verify `Authorization: Bearer <token>` header present
   - On 401 response, verify redirect to /login and tokens cleared from localStorage

3. **No hardcoded fetch calls remain:**
   - Dashboard components use authFetch exclusively
   - All responses properly handled for 401 redirect
</verification>

<success_criteria>
- [ ] routers/deps.py exists with get_current_user and get_filtro_usuario
- [ ] All backend routers import from routers.deps and inject JWT dependency
- [ ] GET endpoints apply role-based filtering: vendedores see only usuario_id=self, admins see all
- [ ] Shared endpoints (catalogo, historico, chat, reportes) require auth but no filtering
- [ ] Proveedores endpoint admin-only (403 for non-admins)
- [ ] authFetch() exported from useAuth.js
- [ ] All dashboard Tab components use authFetch() instead of fetch()
- [ ] 401 responses trigger logout and redirect to /login
- [ ] No bare fetch() calls remain in dashboard components
- [ ] Imports clean: `python -c "from routers import ventas; print('OK')"`
</success_criteria>

<output>
After completion, create `.planning/quick/260401-4hj-apply-role-based-data-filtering-across-a/260401-4hj-SUMMARY.md` with:
- Artifacts created (routers/deps.py, updated routers)
- Frontend changes (authFetch, component updates)
- How to verify role-based filtering works
- Post-merge checklist (test as vendedor and admin)
</output>
